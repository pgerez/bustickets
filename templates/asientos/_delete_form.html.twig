<form method="post" action="{{ path('app_asientos_delete', {'id': asiento.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ asiento.id) }}">
    <button class="btn">Delete</button>
</form>
